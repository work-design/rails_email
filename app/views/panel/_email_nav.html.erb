<div data-controller="menu empty" class="menu ex-accordion is-active">
  <div class="menu-label">
    <div>
      <i class="fa-solid fa-fw fa-envelope"></i>
      <span><%= t('email.panel.title') %></span>
    </div>
    <%= link_to({ controller: 'email/panel/home' }, aria: { label: '设置' }) do %>
      <i class="fa-solid fa-cog"></i>
    <% end %>
  </div>
  <div class="menu-list" data-empty-target="content">
    <%= link_to t('email.panel.logs.index.title'), { controller: 'email/panel/logs' }, data: { turbo_frame: 'body', action: 'menu#enter' }, class: active_helper(controllers: 'logs', active: 'is-active') %>
  </div>
</div>
