<%= render layout: 'index_table', locals: { cache_key: Com::Smtp.column_names.hash } do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @smtps, as: :model %>
<% end %>

<%= paginate @smtps %>
