<%= render layout: 'index_table', locals: { cache_key: LogRecord.column_names.hash } do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @log_records, as: :model %>
<% end %>

<%= paginate @log_records %>
