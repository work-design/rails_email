<%= render layout: 'index_table', locals: { cache_key: LogMailer.column_names.hash } do %>
  <%= render partial: 'index_tbody', collection: @log_mailers, as: :log_mailer %>
<% end %>

<%= paginate @log_mailers %>
